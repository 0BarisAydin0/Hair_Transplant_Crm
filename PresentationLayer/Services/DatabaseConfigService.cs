using DataAccessLayer.Concrate;

namespace PresentationLayer.Services
{
   
        //private readonly Context _context;
        //private readonly IHttpContextAccessor _httpContextAccessor;

        //public DatabaseConfigService(Context context, IHttpContextAccessor httpContextAccessor)
        //{
        //    _context = context;
        //    _httpContextAccessor = httpContextAccessor;
        //}

        //public string ConnectionString
        //{
        //    get
        //    {
        //        var userEmail = _httpContextAccessor.HttpContext.User.Identity.Name;
        //        var databaseConfig = _context.databaseConfigs.FirstOrDefault(c => c.Email == userEmail);
        //        return databaseConfig != null ? databaseConfig.ConnectionString : null;
        //    }
        //}
    
}

@model List<Patient>
@{
    ViewData["Title"] = "Index";
     Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="add-button-top">
    <a href="/Patient/PatientAdd" class="btn btn-success waves-effect waves-light">Hasta Ekle</a>

</div>
<br />
@* 

<table id="example" class="display" style="width:100%">
    <thead>
        <tr>
            <th>Dosya No</th>
            <th>Adı</th>
            <th>Soyadı</th>
            <th>Doğum Tarihi</th>
            <th>Telefon</th>
            <th>Ülkesi</th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {

            <tr>
                <td data-order="1561723222">@item.PatientID</td>
                <td>@item.Name</td>
                <td>@item.Surname</td>
                <td>@item.DateOfBirth.ToShortDateString()</td>
                <td>@item.PhoneNumber</td>
                <td>@item.Country</td>
                <td class="text-center">
                    <div class="d-flex justify-content-center">
                        <div class="btn-group" role="group">
                            <div class="btn-group" role="group">
                                <a asp-action="PatientDetails" asp-route-id="@item.PatientID" asp-controller="Patient" class="btn btn-primary waves-effect waves-light">Detay</a>
                            </div>
                            <div class="btn-group" role="group">
                                <a asp-action="PatientDetails" asp-route-id="@item.PatientID" asp-controller="Patient" class="btn btn-warning waves-effect waves-light">Güncelle</a>
                            </div>
                            <div class="btn-group" role="group">
                                <a href="/Patient/PatientDelete/@item.PatientID" class="btn btn-danger waves-effect waves-light">Sil</a>
                            </div>
                        </div>
                    </div>
                </td>
            </tr>
        }


    </tbody>

</table>



 *@


<!-- Yeni Kayıt Ekle Modal -->
<div class="modal fade" id="newRecordModal" tabindex="-1" aria-labelledby="newRecordModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="newRecordModalLabel">Yeni Kayıt Ekle</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Kapat"></button>
            </div>
            <div class="modal-body">
                <!-- Buraya form elemanlarınızı ekleyin -->
                <form id="newRecordForm">
                    <div class="mb-3">
                        <label for="recordName" class="form-label">Ad</label>
                        <input type="text" class="form-control" id="recordName" required>
                    </div>
                    <div class="mb-3">
                        <label for="recordSurname" class="form-label">Soyad</label>
                        <input type="text" class="form-control" id="recordSurname" required>
                    </div>
                    <!-- Diğer form elemanlarını buraya ekleyin -->
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Kapat</button>
                <button type="button" class="btn btn-primary" id="saveNewRecord">Kaydet</button>
            </div>
        </div>
    </div>
</div>

<body>
    <div>
        <div class="card">
            <div class="card-body">
                <table id="example" class="table table-striped" style="width:100%">
                    <thead>
                        <tr>
                            <th>Dosya No</th>
                            <th>Adı</th>
                            <th>Soyadı</th>
                            <th>Doğum Tarihi</th>
                            <th>Telefon</th>
                            <th>Ülkesi</th>

                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {

                            <tr>
                                <td data-order="1561723222">@item.PatientID</td>
                                <td>@item.Name</td>
                                <td>@item.Surname</td>
                                <td>@item.DateOfBirth.ToShortDateString()</td>
                                <td>@item.PhoneNumber</td>
                                <td>@item.Country</td>
                                <td class="text-center">
                                    <div class="d-flex justify-content-center">
                                        <div class="btn-group" role="group">
                                            <div class="btn-group" role="group">
                                                <a asp-action="PatientDetails" asp-route-id="@item.PatientID" asp-controller="Patient" class="btn btn-primary waves-effect waves-light">Detay</a>
                                            </div>
                                            <div class="btn-group" role="group">
                                                <a asp-action="PatientDetails" asp-route-id="@item.PatientID" asp-controller="Patient" class="btn btn-warning waves-effect waves-light">Güncelle</a>
                                            </div>
                                            <div class="btn-group" role="group">
                                                <a href="/Patient/PatientDelete/@item.PatientID" class="btn btn-danger waves-effect waves-light">Sil</a>
                                            </div>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        }


                    </tbody>

                </table>
            </div>
        </div>
    </div>
</body>
